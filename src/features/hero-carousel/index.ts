/** Public API for the hero-carousel slice — the only entry point other layers may import. */
export { HeroCarousel } from "./components/hero-carousel";
export type { HeroCarouselItem, HeroCarouselLabels } from "./types";
