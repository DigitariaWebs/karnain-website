# Daily Report — 2026-09-07

**Prepared by:** Karnain daily-report agent  
**Live site:** https://karnain-website.vercel.app  
**Live-check method:** curl (with browser `User-Agent`; bare curl returns 403 from Vercel bot-protection on SSR routes)  
**Note:** Notion MCP requires OAuth — report saved to repo; database write skipped (see Blockers).

---

## Shipped today

12 commits on 2026-09-07. 30 files changed, 480 insertions(+), 164 deletions(-).

### feat

| Commit    | Subject                                                    |
| --------- | ---------------------------------------------------------- |
| `03d5d44` | feat(checkout): charge everyone in euros                   |
| `4529152` | feat(supabase): move to a new project and replay the catalog into it |
| `8401c9f` | feat(catalog): pull Rose des Bois from the public catalog  |
| `7307298` | feat(contact): remove the call CTA from the homepage and footer |

### fix

| Commit    | Subject                                                              |
| --------- | -------------------------------------------------------------------- |
| `8875783` | fix(checkout): roll the order back when Stripe refuses the session   |
| `5890caf` | fix(seo): stop the thank-you page repeating the brand in its title   |
| `7b691ca` | fix(checkout): only mark an order paid once the money has actually landed |
| `da57f5e` | fix(checkout): harden the bag → Stripe path                         |
| `03be97c` | fix(admin): show drafts in the back office again                     |

### docs / chore

| Commit    | Subject                                                            |
| --------- | ------------------------------------------------------------------ |
| `7ca0c65` | docs(shop): record the checkout hardening and the return-origin variable |
| `ae00c2f` | chore: format the 2026-09-04 daily report                          |
| `150dd3a` | chore: format daily reports with prettier                          |

**Key additions:** `src/core/stripe/return-url.ts` (new), `src/features/cart/normalise-bag.ts` + tests (new), two Supabase migrations (`20260907104500_draft_rose_des_bois.sql`, `20260907120000_sync_catalog_with_seed.sql`).

---

## Live site

All routes healthy (checked with browser User-Agent). Bare curl without a UA header returns 403 on SSR routes due to Vercel bot-protection — not a site defect.

| Route              | Status | Title                     |
| ------------------ | ------ | ------------------------- |
| `/`                | ✅ 200 | Karnain — Maison de parfum |
| `/collection`      | ✅ 200 | Karnain Addicte · Karnain |
| `/parfums/tobacco` | ✅ 200 | (dynamic title)           |
| `/panier`          | ✅ 200 | Votre panier · Karnain    |
| `/admin/login`     | ✅ 200 | Admin · Karnain           |

**Checkout API:** `/api/checkout` → `{"enabled":true}` — **Stripe is now active.** This is a significant change from the last report where checkout was disabled. Visitors can now proceed through the checkout flow.

No console errors detected; no routes returning errors.

---

## Progress since last report

Delta vs 2026-09-04 daily report:

- **Checkout flipped LIVE** — `/api/checkout` now returns `{"enabled":true}`. Stripe keys have been configured (via `/admin/parametres`). The end-to-end payment path is open to visitors.
- **Checkout hardened** — four fix commits this session tighten the bag→Stripe flow: the webhook now marks an order paid exactly once (idempotent), a failed Stripe session rolls back the order, `return-url.ts` resolves the correct post-payment redirect origin, and currency is locked to EUR.
- **Supabase migrated to a new project** — the catalog was replayed into a new Supabase project (`feat(supabase)`). This likely addressed the security flag around the shared service-role key (rotation-by-replacement).
- **Rose des Bois confirmed as draft** — `feat(catalog)` adds a migration that formally sets Rose des Bois to draft status in the new Supabase project. It is hidden from the public catalog; visible in admin. The 2026-09-04 report noted this as a blocker — it is now codified in the DB.
- **Call CTA removed** — the phone number / "Appeler" CTA has been stripped from the homepage and footer, cleaning up the placeholder contact copy.
- **Admin draft visibility fixed** — a regression caused drafts to disappear from the admin product list; fixed today.
- **`normalise-bag` utility added** — new tested module that normalises cart line quantities before sending them to Stripe, with 65 lines of unit tests.

---

## In progress

No open spec branch detected. All `tasks.md` files for specs 001–012 are fully checked off. The launch report marks all 11 features as **Live**.

Potential upcoming work (based on launch report "Outstanding" section):
- Legal/CGV copy — still pending (blocks full public sale)
- Product photography for Rose des Bois (draft; needs an approved photo before republishing)
- Domain cutover (`karnain.fr` → Vercel)

---

## Next

1. **Verify checkout end-to-end in production** — now that `enabled:true`, run a test purchase with a Stripe test card to confirm the live Supabase + live Stripe webhook path works (new Supabase project = new webhook endpoint URL to register).
2. **Register the Stripe live webhook** pointing at the new Vercel deployment URL (`/api/stripe/webhook`).
3. **Add Legal/CGV copy** and finalize contact details before public sale.
4. **Upload Rose des Bois bottle photo** from `/admin` once an approved image is available.
5. **Point `karnain.fr`** at the Vercel project.
6. **Authorize Notion MCP** in an interactive session (claude.ai → Settings → Connectors) so daily reports write to the database.

---

## Blockers

- **Notion MCP unauthenticated** — the Notion connector requires OAuth and cannot be authorized in a non-interactive scheduled session. Daily report saved to repo only; "Daily Reports" database (`ad46a915-34bc-4f29-8b94-d0789a062198`) not updated. **Action:** authorize at claude.ai → Settings → Connectors in an interactive session. (Persists from 2026-09-01.)
- **Stripe webhook registration** — the Supabase project migration likely changed the effective deployment; the live webhook endpoint in Stripe must be verified/updated to point at the current Vercel URL. Unverified; checkout is enabled but webhook delivery is uncertain on the new project.
- **Legal/CGV** — copy pending before public sale can open.
- **Rose des Bois** — no approved bottle photo; product stays as draft.
